# Ejecucion en modo development
Iniciar el servidor
```bash
cd server
npx json-server --watch db.json --routes routes.json --port 3000
```
Iniciar la aplicacion

```bash
npm run dev
```profile-api.service