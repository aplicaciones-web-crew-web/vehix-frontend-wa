export default class ResourceCard {
    constructor({ id, titleKey, icon, route }) {
        this.id = id;
        this.titleKey = titleKey;
        this.icon = icon;
        this.route = route;
    }
}